#include<iostream>
#include<iomanip>
#include"List.h"
using namespace std;

int main()
{
   /* List values(10);
    List count(10);
    int value ; 
    int counter = 0;
    bool flag = 0 ;
    cout<<"enter values -1 for exit\n";
    cin >> value ;
    while (value != -1)
    {
        for(int i = 0 ; i < values.getSize() ; ++i)
        {
            if(value == values[i])
            {
                count[i] ++ ; 
                flag = 1 ;      
                counter ++;
                break ;
            }
        }
      if(! flag)
      {
        values[counter++] = value;
      } 
      flag = 0 ;
      cin >> value ;
    }
    cout<<"value"<<"    "<<"frequency"<<'\n';
    for(int i = 0 ; i < values.getSize();i++)
    {
        if(values[i])
        cout<<setw(3)<<values[i]<<"         "<<setw(3)<<count[i]+1<<endl;
    }
 */
cout<<fixed<<setprecision(3)<<2.144555<<endl;
cout<<2.555555<<endl;
}